import { StyleSheet } from "react-native";

const DStyle = StyleSheet.create({
  flexDColumn: { display: "flex", flexDirection: "column" },
  flexDRow: { display: "flex", flexDirection: "row" },
});
export default DStyle;
